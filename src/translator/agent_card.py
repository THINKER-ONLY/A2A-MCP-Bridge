# 占位符：AgentCard 定义

# from common.types import AgentCard, AgentCapabilities, AgentSkill # A2A common 类型

# def get_mcp_gateway_agent_card(host: str, port: int) -> AgentCard:
#     # 定义能力 (capabilities), 技能 (skills) 等
#     # capabilities = AgentCapabilities(streaming=False, pushNotifications=False)
#     # skill = AgentSkill(
#     #     id="execute_mcp_command",
#     #     name="执行MCP命令",
#     #     description="通过MCP协议调用下游服务的方法。",
#     #     inputModes=["data"],
#     #     outputModes=["data"]
#     # )
#     # return AgentCard(...)
#    pass 